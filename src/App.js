import AdminPanel from "./AdminPanel"

function App() {
  return (
    <div>
      <AdminPanel/>
    </div>
  );
}

export default App;



//step 1 : getting the data dynamically using the api
//step 2 : putting the information wrapper dynamically